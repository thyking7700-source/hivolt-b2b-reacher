export function OutreachEngine() {
  return (
    <section className="rounded-2xl bg-card p-6 shadow-[var(--shadow-border)]">
      <h2 className="text-sm font-semibold">Reach-out</h2>
      <p className="mt-2 text-sm text-muted-foreground">Reach-out tab is being copied onto this public build. Extract tab works now.</p>
    </section>
  );
}
